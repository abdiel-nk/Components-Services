import { FeaturesDirective } from './features.directive';

describe('FeaturesDirective', () => {
  it('should create an instance', () => {
    const directive = new FeaturesDirective();
    expect(directive).toBeTruthy();
  });
});
